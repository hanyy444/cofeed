import React from 'react';
import './spinner.component.scss'

const Spinner = (props) => {
    return ( 
        <div className= "spinner" id="spinner" data-testid="spinner" />
    )
}

export default Spinner;

